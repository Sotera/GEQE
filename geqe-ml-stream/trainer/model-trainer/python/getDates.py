import sysimport jsonimport tangeloimport osimport datetime@tangelo.restfuldef get(filePath='../../../',subDir='raw_tweet_data/analyzed/'):    scoreDir = filePath + subDir    lFiles = sorted(os.listdir(scoreDir))    dates = set()    for file in lFiles:        date = file[:10]        dates.add(date)    retDict = {'dates':sorted(list(dates)), 'nDates':len(dates)}    print retDict    return json.dumps(retDict)